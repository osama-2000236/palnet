# Missing Flows — parity matrix (audit 2026-07-03)

Audit of the "Missing Flows" design bundle against the repo. Most flows were
already implemented in earlier sprints; this pass closed the two concrete
gaps found and records the rest.

| #   | Flow                                                | Web                                                             | Mobile                                              | Status                    | Notes                                                                                                                                                                                                                                                                                           |
| --- | --------------------------------------------------- | --------------------------------------------------------------- | --------------------------------------------------- | ------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | State system (EmptyState / Illustration / Skeleton) | `packages/ui-web`                                               | `packages/ui-native`                                | **Verified**              | 10 motifs × 3 direction kits, tokenized, twins share prop names.                                                                                                                                                                                                                                |
| 2   | Onboarding (5 steps)                                | `(app)/onboarding` + `(auth)/*`                                 | `(app)/onboarding` + `_onboarding/`                 | **Verified**              | 5-step contract documented in web page (steps 1–2 live in auth).                                                                                                                                                                                                                                |
| 3   | New-user home                                       | `(app)/feed`                                                    | `(app)/feed` + `_feed/`                             | **Fixed this pass**       | Web feed rendered a dead placeholder form and had no completeness card. Wired the existing `Composer` (optimistic prepend) and added `ProfileCompletenessCard` — same 5-item checklist as mobile `ProfileSummary`. Starter-people rail already existed (`RightRail` pymk / mobile suggestions). |
| 4   | Messaging — thread, attachments, read receipts      | `(app)/messages`                                                | `(app)/messages` + `_message-thread/`               | **Verified**              | SSE transport, thread states covered.                                                                                                                                                                                                                                                           |
| 4b  | Messaging — Focused/Requests tabs + request gating  | —                                                               | —                                                   | **Blocked (product)**     | No backend support: the messaging module requires accepted connections to DM, so there is no "request" object to tab on. Shipping the tabs needs an API/product decision (message-request entity + safety gating) before any UI.                                                                |
| 5   | Jobs & employers — company page, applicant tracking | `(app)/employer/*`                                              | `(app)/employer/*`, `(app)/company/*`               | **Verified**              | Applicant inbox with status transitions on both platforms.                                                                                                                                                                                                                                      |
| 5b  | Jobs & employers — post a job                       | `employer/[slug]/jobs/new`                                      | `employer/[slug]/jobs/new`                          | **Fixed this pass**       | Native twin added: same POST body contract, RadioGroup pickers reuse `jobs.typeLabels`/`jobs.locationLabels`, entry button on employer jobs screen. `expiresAt` omitted on native (no date-picker primitive yet) — **intentional exception**, server default applies.                           |
| 5c  | Employer create (company)                           | `employer/new`                                                  | —                                                   | **Intentional exception** | Company creation stays web-only for now; mobile links out. Revisit if operator data shows demand.                                                                                                                                                                                               |
| 6   | Premium & monetization                              | `me/premium`, `employer/[slug]/billing`, `components/billing/*` | `me/premium`, `employer/[slug]/billing`, `_karama/` | **Verified**              | Method picker (card / bank transfer / POINTS personal-only / wallets), wallet tiles gated by `WalletAvailability` (coming soon), invoices in both scopes.                                                                                                                                       |
| —   | Payment-rails reference                             | `billing/wallets/*.client.ts`                                   | —                                                   | **Verified**              | JawwalPay / PalPay / reflect stubbed `NOT_IMPLEMENTED`; no fake successes.                                                                                                                                                                                                                      |

## QA (this pass)

- `pnpm lint:tokens` — clean
- `pnpm format:check` — clean
- `pnpm lint` (web + mobile) — clean
- `pnpm type-check` (web + mobile) — clean
- `pnpm test` — web 21/21, mobile 88/88 (tabs-layout test updated route registry)
- `pnpm check:release-placeholders` — clean
- `pnpm --filter @baydar/db generate` — clean
- `pnpm --filter @baydar/web test:a11y` — **not run**: Playwright web server needs `DATABASE_URL` / `JWT_*` env vars, absent in this environment (known env/ops blocker)
- `pnpm mobile:recovery-check` — clean
