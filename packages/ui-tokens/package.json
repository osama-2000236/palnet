{
  "name": "@baydar/ui-tokens",
  "version": "0.0.1",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -p tsconfig.json --watch",
    "clean": "rimraf dist",
    "lint": "eslint \"src/**/*.ts\" \"tailwind-preset.ts\"",
    "type-check": "tsc --noEmit"
  },
  "devDependencies": {
    "@baydar/config": "workspace:*",
    "rimraf": "^6.0.1",
    "typescript": "^5.6.0"
  }
}
